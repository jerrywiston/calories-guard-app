# 版本紀錄

## 2026-09-24

- 調整圖片報告的備註份量呈現：舊紀錄會把能明確對應的份量直接加到原品項後，例如「白飯（四分之一碗）」，不再另列「備註份量」；備註中無法對應原品項的文字與沒有份量的品項不額外顯示。
- 修正圖片報告無法辨識「四分之一碗」等份量：備註份量判斷新增中文分數、阿拉伯分數、小數與分數符號格式（例如「三分之二碗、1/4 碗、0.25 碗、¼ 碗」），Gemini 提示也加入中文分數範例；Web、GitHub Pages 與 Android 的分析快取與離線快取同步更新。
- 圖片報告的品項名稱新增備註份量：Gemini 會為每個品項保留使用者明確寫出的「一條、半碗、150 公克、兩顆」等 `portion_description`，報告顯示為「香蕉（一條）」、「白飯（半碗）」。分析快取版本同步提升，確保舊快取會重新分析；舊歷史紀錄若沒有結構化份量欄位，則在品項區補列含份量的餐次備註。Web、GitHub Pages 與 Android 共用此行為。
- 補齊 Git 忽略規則，排除 Android／Capacitor／自訂相簿 plugin 的 Gradle `build`、`.gradle` 產物，以及 iOS 的 `build`、`Pods`；避免 `annotation` 等深層編譯檔案因 Windows 路徑過長而被複製或提交。
- 修正 GitHub Pages／一般手機瀏覽器的右上角設定對話框無法滑到最底部：設定視窗改用動態視窗高度與獨立垂直捲動，阻止手勢穿透到背景頁面，並將清除／儲存按鈕固定在設定視窗底部且納入安全區留白。Android 專案未同步、既有 APK 不變。
- 新增 GitHub Pages 純靜態網站模式：在 `github.io` 上由瀏覽器直接呼叫 Gemini，使用 IndexedDB 保存日期 JSON 等價資料與已產生報告，不再依賴 Node.js `/api`；加入歷史 JSON 備份匯出／匯入、PWA manifest、離線 App shell、專案子路徑相容資源網址及 GitHub Actions Pages 部署流程。Android 原生分支與現有 APK 不變。
- 新增 `memory.md`，統整目前產品需求、Web／Capacitor 架構、Gemini 分餐與快取流程、歷史資料格式、現行報告排列演算法、已撤回方案、平台限制、建置驗證方式與後續接手原則，供其他 session 快速恢復專案脈絡。
- 照片數量上限提高為每餐 10 張、每天 30 張，網頁、Android、伺服器驗證、介面提示與說明文件同步更新。
- 報告內每餐超過 6 張照片時改用有限候選排列：比較循環位移及依比例、品項權重、寬高需求排序的候選，不再計算最多 `10!` 種排列；6 張以下仍使用完整排列搜尋。

## 2026-09-23

- 二維背包排版改用部分順序約束：早餐、午餐、晚餐維持時間順序，下午茶與宵夜會窮舉插入所有可用位置，再選擇總高度與空白面積評分最低的排列。
- 再提高照片縮放容忍度，單品項與多品項照片的最低短邊分別降至 120px、135px，理想縮放門檻降至 58%、32%；未使用面積的評分權重由 0.22 提高為 0.55。
- 依使用回饋將全天報告退回單一矩形二維背包排版：移除 Polyomino、Guillotine 與匯出版型選擇器；每餐只產生不同寬度的矩形候選，再由 Skyline 全局搜尋總高度較短、空白較少的組合。
- 保留依照片品項數加權面積的規則，多品項主餐照片會取得較大面積，單品項飲料與零食可使用較小餐卡；同時保留縮小後的報告文字、緊縮底部留白與較寬鬆的照片縮放範圍。
- 收緊兩種報告版型的餐卡內容：餐次標題、照片數量與品項文字縮小，照片到品項的間距及底部保留量同步降低，餐卡高度改用 20px 細網格，減少文字下方因高度進位產生的空白。
- 放寬報告照片縮放容忍度：單品項與多品項照片都允許縮小到更靈活的尺寸，並降低未達理想尺寸時的評分懲罰，讓全局網格與 Guillotine 能使用更多緊密排列候選。
- 匯出報告新增「全局網格」與「Guillotine 切割」兩種正式版型，可在匯出視窗選擇並記住上次使用的方式；儲存、下載與分享都會採用目前選定的排版。
- 新增保留餐次順序的 Guillotine 動態規劃：針對連續餐次窮舉水平與垂直切割、餐卡尺寸及左右對齊位置，依總高度、未使用面積與照片可讀性選擇結果。
- 報告排版升級為 12 欄、40px 列高的有順序加權 Polyomino 搜尋：每餐保持單一連通區域，品項臂可靠左、置中或靠右，並提高未使用面積的評分懲罰；在維持早餐到宵夜順序及多品項照片較大的條件下，優先選擇總高度與空洞最少的排列。
- 全天報告加入受限制的 L 型餐次候選版面：寬幅照片區下方的品項文字可形成左臂或右臂，其他餐次能填入凹角；Skyline 會和矩形候選一起比較整體高度與空白，只在更省空間時採用 L 型。
- 重做照片與報告預覽操作：標題列、可捲動圖片區與底部縮放列分開配置，不再互相遮擋；改由檢視器處理單指拖曳、雙指縮放，並新增縮小、100% 重設與放大按鈕，縮放範圍為 100%～400%。
- 報告頂部的總熱量改為數字與 `kcal` 上下排列，建議值比較同步下移，避免較長熱量數字把單位推過中間分隔線。
- 多品項主餐改用彈性面積範圍：仍依品項數維持比單品項照片更大的面積，但可在能顯著減少報告高度或空白時縮小一級餐卡，讓 Skyline 更容易把不同餐次緊密拼接。
- 收斂報告頂部營養總覽的字級並重新調整欄寬、列距與卡片高度，避免熱量、營養數值和建議比較文字互相擁擠或重疊。
- 單品項照片允許使用更小的最低尺寸與 240px 窄餐卡，讓飲料、零食等簡單餐次能與其他餐次更密集地拼接；多品項主餐仍保留較大的照片與餐卡權重。
- 報告排版新增逐張照片的品項數權重：Gemini 會回傳照片內可辨識的品項數，多品項主餐取得較大照片面積，單一零食與飲料使用較小面積；權重採平滑上限並保留最低可辨識尺寸，避免極端放大或縮小。舊紀錄沒有品項數時會依照片角色與整餐品項推估。
- 再次調整全天報告的閱讀比例：日期、主標題、熱量、六項營養、餐次與品項文字提高約 35～50%，讓報告經 LINE 縮圖或壓縮後仍能清楚辨識；同步加高總覽與餐次卡片，避免大字互相重疊。
- 全天營養報告維持 1440px 短邊，避免為了過高解析度增加不必要的檔案大小。
- 報告改為高品質 JPEG，依內容逐步調整壓縮品質並以不超過約 2 MB 為目標，減少 LINE 再次大幅壓縮的機會。
- 放大報告的日期、標題、熱量、營養數值、建議值比較、餐次名稱與完整品項文字，提高經通訊軟體壓縮後的可讀性。
- 新版 JPG 報告仍可讀取舊版 PNG 預覽；重新匯出時會清除舊版內部報告檔。

## 2026-09-22

- 每一餐改為「餐次、共用備註、可收合照片、營養分析」的排列。
- 照片區預設收起；新增或拍攝照片後會自動展開，歷史紀錄與 App 啟動時維持收起。
- 餐次營養分析移到該餐照片區下方，且不受照片收合狀態影響。
- Android 與 iOS 的拍照按鈕改用 Capacitor Camera，直接開啟原生後鏡頭；網頁版保留瀏覽器相機輸入。
- 頁尾新增「移到頂部」按鈕。
- 從日曆載入日期、建立空白日期或啟動 App 時，畫面固定回到頁面頂端。
